#include "main.h"

/**
 * _isdigit - fuction that check if a number is digit from 0-9
 * @c: integer parameter
 * Return: integer number
 **/
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
return (1);
return (0);
}

